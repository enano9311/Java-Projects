package CardGame;
import CardGame.FaceValue;
import CardGame.Suit;
import CardGame.Card;

public class CardMath {
    // Convert FaceValue enum into int so we can do math. Jack -> 11, for instance

}
